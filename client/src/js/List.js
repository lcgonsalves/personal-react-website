import React, { Component } from 'react';
import '../css/List.css'

const List = (props) =>
{
    let cssClassName = 'list';

    return (
        <div>this is a list</div>
    );
};

export default List;